# Known Boundaries

- Windows Docker Desktop upgrade, restart, backup/restore and rollback must be executed on Dave's PC. This environment cannot claim that gate passed.
- Microsoft, Google and SMTP/IMAP adapters are code-complete and mock-tested; live credentials, provider app registration/approval and customer consent are external activation dependencies.
- AI safe-template mode is local and testable. Live AI provider credentials/cost controls are external activation dependencies.
- Social native publishing is intentionally not included. Customer copy/paste is the frozen v5.1 scope.
- Legal counsel review of MSA/Order Form remains outside code acceptance.
- Hasan handoff remains blocked until Product Owner manual QC passes.
