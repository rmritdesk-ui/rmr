# RMR Global v5.4 Tenant Themes — Release Certification

## Candidate identity

- Release: `5.4.0-tenant-themes-po1`
- Source baseline: sealed `5.3.1-final-production-corrections-po1`
- Canonical v5.3.1 SHA-256: `d188a52eab6494cb61bcecd828616d4bc17a1fa30cd5ddd2ff54e28555b5f1bf`
- Additive migration: `005.006.000-tenant-themes`
- Isolated Windows/Docker Product Owner port: `8085`

## Authorized implementation

v5.4 adds tenant-scoped branding to authenticated RMR Global workspaces: a protected tenant logo, primary/secondary/accent colors, computed readable foregrounds, a bounded set of safe typography choices, a live preview, save/apply, logo removal, reset to the RMR default, and RMR Administrator/own-tenant Client Administrator management.

The public website architecture is unchanged. Kerry’s managed public website and external website connections remain outside the Tenant Themes scope. RMR Global ownership attribution remains visible.

## Pre-Windows validation

- Functional and business-outcome QC: **200 passed / 0 failed**
- Controlled restart persistence: **10 passed / 0 failed**
- v5.3.1-to-v5.4 upgrade/data preservation: **10 passed / 0 failed**
- Frozen scope gate: **30 passed / 0 failed**
- Windows launcher/Compose static contract: **23 passed / 0 failed**
- Tenant Themes UI static gate: **17 passed / 0 failed**
- Python tests: **23 passed / 0 failed**
- JavaScript syntax checks: **7 files passed / 0 failed**
- Python compile validation: **passed**

The build environment does not expose a Docker engine. Its Chromium policy also blocks navigation to local application URLs with `ERR_BLOCKED_BY_ADMINISTRATOR`. These are recorded as environment limitations rather than application failures. The one-click launcher and Compose contract were statically verified, while the running application passed direct HTTP/API, database, migration, tenant-isolation, and controlled-process restart checks.

## Remaining acceptance gate

This is not a production approval or Hasan/Hameer handoff. Dave must run `START-PRODUCT-OWNER-TEST.bat` on Windows with Docker Desktop, complete the Tenant Themes Product Owner Golden Path, and explicitly approve v5.4.

## Clean extraction and package controls

A clean archive was extracted to a new `RMR540` directory. Its controlled payload passed the package manifest, compilation, JavaScript syntax, 23 Python tests, v5.4 scope, launcher, UI, exact baseline-preservation, full 200-check functional QC, and 10-check controlled-restart persistence gates. The final downloadable archive receives an external SHA-256 after evidence sealing.
